import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',

    justifyContent: 'center',
    alignItems: 'center',

    gap: 32,
  },
  containerHome: {
    flex: 1,
    backgroundColor: '#fff',
  },
  primaryButton: {
    color: "#5FD068",
  }
});