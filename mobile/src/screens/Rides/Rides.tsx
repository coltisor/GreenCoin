import React from 'react';
import { View } from "react-native";
import {RidesScreenProps} from "../RouteProps";

export const Rides = (props: RidesScreenProps) => {
  console.log('RIDES');
  return (
    <View>RIDES</View>
  )
}
